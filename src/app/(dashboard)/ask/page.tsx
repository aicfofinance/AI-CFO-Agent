import type { ReactElement } from "react";

export default function AskPage(): ReactElement {
  return (
    <div>
      <h1 className="text-3xl font-semibold text-gray-900">Ask</h1>
    </div>
  );
}
